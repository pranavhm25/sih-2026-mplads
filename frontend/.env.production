# Production build configuration — read by Vite at build time.
# This is a public URL (the API is meant to be called from browsers), so
# baking it into the frontend bundle is intentional and safe.
# If the backend URL ever changes, update this line and redeploy.
VITE_API_URL=https://drishti-backend-h2c8.onrender.com
