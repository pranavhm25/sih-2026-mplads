# Backend API URL for production deployment (e.g. on Render)
# Example: VITE_API_URL=https://drishti-backend-h2c8.onrender.com
# When running locally with Vite proxy or Docker reverse-proxy, leave blank.
VITE_API_URL=
