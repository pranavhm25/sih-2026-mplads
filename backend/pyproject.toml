[project]
name = "drishti-backend"
version = "0.1.0"
description = "Drishti — MPLADS Risk Intelligence & Investigation Platform (backend)"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
filterwarnings = [
    "ignore::DeprecationWarning",
]
